POSTGRES_HOST=db # Do not change this
POSTGRES_PORT=5432 # Do not change this
POSTGRES_USER=sentimenta
POSTGRES_DB=sentimenta
POSTGRES_PASSWORD=sentimenta

# for CORS (comma-separated, include http:// or https://)
ALLOWED_ORIGINS=

# generate https://jwtsecret.com/generate or use `openssl rand -hex 256`
JWT_SECRET=secret

PUBLIC_GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CLIENT_CALLBACK=http://api_host/auth/google/callback

PUBLIC_GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_CLIENT_CALLBACK=http://api_host/auth/github/callback

GRAFANA_USER=admin
GRAFANA_PASSWORD=admin

# From openrouter
AI_API_KEY=
AI_MODEL=

PUBLIC_AI_ENABLED=true

PUBLIC_PASSWORD_LENGTH_MIN=8
PUBLIC_MOOD_DESC_LENGTH_MAX=160
PUBLIC_MOOD_EMOTES_LENGTH_MAX=80

PUBLIC_REGISTRATION_ENABLED=true